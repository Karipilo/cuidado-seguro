// src/utils/initLocalStorage.js

// Esta función está desactivada porque ahora los pacientes
// se obtienen desde el backend real y no desde localStorage.

export function inicializarLocalStorage() {
  // Función intencionalmente vacía
  return;
}
